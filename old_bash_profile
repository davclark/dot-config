# This is relevant to stuff compiled with the intel math kernel lib, e.g. EPD
# MKL_NUM_THREADS=1
# export MKL_NUM_THREADS

# Might need these sometime, but not now
# export PYTHONPATH=~/Code/pythonlibs:/usr/local/lib/python2.7/site-packages
export EDITOR=vim

## I'm not sure what this is, but it's not cool to set my TMPDIR file without
## asking me!
# New environment setting added by Mplus Version 7 Base 
# Do NOT modify these lines; they are used to uninstall MPlus Version 7 Base Program 
# and Combination Add-On (Mac OS X 64-bit) (Sun Oct 21 21:43:50 PDT 2012)
# TMPDIR="/private/var/folders/k6/ttm9kby16pq62t407fgpm4gm0000gn/T/Cleanup At Startup"
# export TMPDIR

export CLICOLOR=1
export GREP_OPTIONS='--color=auto'
PATH=/opt/local/bin:/opt/anaconda/bin:/opt/packer:/usr/local/bin:/Applications/git-annex.app/Contents/MacOS:$PATH
export PATH=$PATH:/usr/local/share/npm/bin:/Applications/Mplus

# This breaks stuff if in the path: /opt/anaconda/bin
# So we set up some aliases instead
# The activate script sets $CONDA_DEFAULT_ENV
# I just do `. activate` and `. deactivate` now that I'm making anaconda my
# default
# alias anactivate='source /opt/anaconda/bin/activate'
# alias deana='source /opt/anaconda/bin/deactivate'

# Other aliases for stuff I like

alias glo='git log --stat ORIG_HEAD..'
alias skim='open -a Skim'

# Git doens't like wrapping lines. I do.

export LESS=FRX

if [ -f $(brew --prefix)/etc/bash_completion ]; then
  . $(brew --prefix)/etc/bash_completion
fi

# Don't use this anymore, as conda works nicer
# Virtualenv wrapper (built on system python)
# [[ -s /usr/local/bin/virtualenvwrapper_lazy.sh ]] &&
#     source /usr/local/bin/virtualenvwrapper_lazy.sh

# Load RVM into a shell session *as a function*
# We don't use RVM anymore because it's insane
# [[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

# rbenv is much more sane
if which rbenv > /dev/null 
then 
    eval "$(rbenv init -)"
fi


